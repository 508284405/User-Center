<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyue.usercenter.infrastructure.user.mapper.UserMapper">
    
    <select id="selectByUsername" resultType="com.leyue.usercenter.infrastructure.user.dataobject.UserDO">
        SELECT * FROM user WHERE username = #{username} and is_deleted = 0 LIMIT 1
    </select>
    
    <select id="selectByGoogleId" resultType="com.leyue.usercenter.infrastructure.user.dataobject.UserDO">
        SELECT * FROM user WHERE google_id = #{googleId} and is_deleted = 0 LIMIT 1
    </select>
    
    <select id="selectByRoleId" resultType="com.leyue.usercenter.infrastructure.user.dataobject.UserDO">
        SELECT u.* FROM user u
        INNER JOIN user_role ur ON u.id = ur.user_id and ur.is_deleted = 0
        WHERE ur.role_id = #{roleId} and u.is_deleted = 0
    </select>
    
    <select id="selectByRoleCode" resultType="com.leyue.usercenter.infrastructure.user.dataobject.UserDO">
        SELECT u.* FROM user u
        INNER JOIN user_role ur ON u.id = ur.user_id and ur.is_deleted = 0
        INNER JOIN role r ON ur.role_id = r.id and r.is_deleted = 0
        WHERE r.role_code = #{roleCode} and u.is_deleted = 0
    </select>
    
</mapper> 