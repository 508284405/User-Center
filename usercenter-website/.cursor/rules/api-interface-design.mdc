---
description: 创建api文件，定义api接口
globs: 
alwaysApply: false
---
# 管理端 API 接口设计规则

本规则针对项目中管理后台（运营/客服）使用的后端 API 接口设计提供统一规范，确保路径、方法、参数和响应格式一致、可维护。

## 1 路径与命名
- 所有管理端业务统一前缀 `/aftersale/admin/`（或 `/api/admin/aftersale/`，与网关配置保持一致）。
- 资源使用复数或业务名称小写驼峰，如售后 (`aftersale`)、退款 (`refund`)。
- 分页查询接口使用 `/resource/page` 形式，如：
  - **分页查询售后单**  [AfterSalesList](mdc:smartcs-web/smartcs-web/src/api/afterSales.ts)  → `GET /aftersale/admin/page`
  - **分页查询退款记录** [AfterSaleRefund](mdc:smartcs-web/smartcs-web/src/api/afterSaleRefund.ts) → `GET /aftersale/admin/refund/page`

## 2 HTTP 方法
- `GET`：查询、分页列表、详情接口
- `POST`：创建、修改、操作（审核、处理）
- `DELETE`：删除资源（如需）

## 3 请求参数
1. 分页列表接口：
   - `pageIndex` (int) 当前页，从1开始
   - `pageSize` (int) 每页条数
   - 业务筛选参数使用驼峰，如 `afterSaleNo`、`status`、`orderNumber`。
   - 可选时间筛选以时间戳形式传递，如 `startTime`、`endTime`。
2. 操作接口（审核、拒绝、处理退款）：
   - 路径参数或请求体中包含唯一标识，如 `refundId`、`afterSaleNo`。
   - JSON Body 传递额外信息，如审核结果 `auditResult`、备注 `comment` 或拒绝原因 `reason`。

## 4 请求体格式
- 通用使用 `application/json`，对复杂对象或数组使用 JSON。
- 上传文件接口必须设置 `multipart/form-data`。

## 5 响应格式
- 统一包装：
  ```json
  {
    "success": true,
    "errCode": "",
    "errMessage": "",
    "data": ...,          // 单对象或数组
    "totalCount": 0,      // 列表接口返回
    "pageSize": 10,
    "pageIndex": 1
  }
  ```
- 列表接口：`data` 为数组
- 详情接口：`data` 为对象
- 操作接口（无返回数据时）：`data` 可省略或为 `null`

## 6 错误与异常
- 后端返回 `success=false` 时，前端统一提示 `errMessage`
- 网络或 HTTP 错误，提示通用 `网络异常，请重试`。

## 7 示例 & 参考
- 查看实际接口定义：
  - [src/api/afterSales.ts](mdc:smartcs-web/smartcs-web/src/api/afterSales.ts)
  - [doc/AdminAfterSaleControllerApi.md](mdc:smartcs-web/smartcs-web/doc/AdminAfterSaleControllerApi.md)
- 具体使用示例见组件：
  - [AfterSalesList.vue](mdc:smartcs-web/smartcs-web/src/views/after-sales/AfterSalesList.vue)
  - [AfterSalesDetail.vue](mdc:smartcs-web/smartcs-web/src/views/after-sales/AfterSalesDetail.vue)
